import template from './types.html';
import controller from './types.controller';
import './types.scss';

let typesComponent = {
  bindings: {},
  template,
  controller
};

export default typesComponent;
